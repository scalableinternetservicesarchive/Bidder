<%= form_for(user_community) do |f| %>
  <% if user_community.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_community.errors.count, "error") %> prohibited this user_community from being saved:</h2>

      <ul>
      <% user_community.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :community_id %>
    <%= f.number_field :community_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
